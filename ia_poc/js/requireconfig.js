var require = {
    paths : {
        // jquery maps to js/jquery-2.0.0.js, don't need to add the '.js' and as both these files are in the same dir don't need to add path (Comma separate for multiple items)
        "jquery" : "jquery-1.9.1",
        "mustache" : "mustache",
        "text" : "text",
        "abpify" : "abpify"
    }
}